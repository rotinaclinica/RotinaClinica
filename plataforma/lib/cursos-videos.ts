// Server-only: never import this from client components ("use client").
// YouTube IDs are served exclusively via /api/cursos/[id]/embed (auth required).
export const YOUTUBE_IDS: Record<number, string> = {
  1: "APZqdnpNIjA",
};
